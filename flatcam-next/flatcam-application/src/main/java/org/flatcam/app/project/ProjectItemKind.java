package org.flatcam.app.project;

/** Mirrors the object kinds in the legacy app's ObjectCollection (appObjects/). */
public enum ProjectItemKind {
    GERBER,
    EXCELLON,
    GEOMETRY,
    CNC_JOB,
    DOCUMENT
}
